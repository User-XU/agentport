# CLAUDE.md

Claude entrypoint for this repository.

Read `README.md`, `docs/architecture.md`, `docs/quickstart.md`,
`docs/usage-recipes.md`, and `SCHEMA.md` before changing files. Follow the same
repository rules defined in `AGENTS.md`.

This project is an independent portable agent context system. Do not collapse it
into a single skill or prompt file. The skill under `skills/` is only an adapter
that teaches agents how to use the project.
